import React from "react";

const OrderDetailContainer = (props) => {
    return (
        <div
            className="w-full h-full p-4 flex flex-col gap-2 overflow-y-auto"
            {...props}
        />
    )
}

export default OrderDetailContainer