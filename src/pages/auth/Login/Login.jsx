import React from "react";

export const ActionWrapper = (props) => {
    return(
        <div className="mt-5 flex items-center w-full justify-between text-sm font-bold opacity-60" {...props}/>
    )
}